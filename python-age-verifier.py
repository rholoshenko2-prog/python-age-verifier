name=input('Как тебя зовут?')

print(' Привет, ' + name )

age= input('Сколько тебе лет? ').strip()

if age.isdigit():
    age=int(age)

    if age>=18:
        print ('Добро пожаловать, ' + name + '! Ты взрослый, заходи!')
    else:
        age_st=18-age

        if age_st == 1:
            print('Извини, ' + name + ' , тебе ещё рано. Приходи через 1 год! 😊')
        elif age_st % 10 in [2, 3, 4] and age_st % 100 not in [12, 13, 14]:
            print('Извини, ' + name + ', тебе ещё рано. Приходи через ' + str(age_st) + ' года! 🔥')
        else:
            print('Извини, ' + name + ' , тебе ещё рано. Приходи через ' + str(age_st) + ' лет! 💪')
    # Один общий лог в конце (всегда выводится после результата)
    print('\n--- Результат проверки ---')
    print('Имя: ' + name)
    print('Возраст: ' + str(age))  # ← оригинальный ввод
    if age >= 18:
        print('Результат: Доступ разрешён ✅')
    else:
        if age_st == 1:
            print('Доступ: запрещён (ещё ' + str(age_st) + ' год) ⏳')
        elif age_st % 10 in [2, 3, 4] and age_st % 100 not in [12, 13, 14]:
            print('Доступ: запрещён (ещё ' + str(age_st) + ' года) ⏳')
        else:
            print('Доступ: запрещён (ещё ' + str(age_st) + ' лет) ⏳')
    print('-------------------------')

else:
    print('Возраст должен быть числом! Попробуй заново.')

